We received a request to reset your ProjectTwo credentials. To do so, please follow the link below:

<%= @host %>/reset/<%= @user.code %>

If you received this email in error, simply do nothing. The above link will expire at <%= @user.expires_at.strftime("%-I:%M on %a, %-d %b %Y") %>.

Thanks!
ProjectTwo